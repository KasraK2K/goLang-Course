package __basics

import (
	"fmt"
)

func String() {
	fmt.Println("Learn Go Strings and Runes")

	/* String */
	/*
		//--------------------------------------------------
		var text string = "some sample text"

		var textLength int = len(text)
		fmt.Println("Len:", textLength)

		for i := 0; i < textLength; i++ {
			fmt.Printf("%x ", text[i])
		}
		fmt.Println()

		for index := range text {
			fmt.Printf(string(text[index]))
		}
		fmt.Println()

		fmt.Println(text[:4], text[7:8], text[12:])
		//--------------------------------------------------
	*/
}
